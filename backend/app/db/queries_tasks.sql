-- Optional reference-only file (not executed automatically).
-- Keep SQL you like to reuse in services.

-- List tasks visible to a user (admin sees all, staff sees only assigned)
-- Implemented in Python services for clarity.
